package danbikel.parser;

import danbikel.switchboard.*;
import java.rmi.*;

/**
 * An interface to serve as a semantic marker that this is a parsing client for
 * a {@link Switchboard} instance.
 */
public interface ParserRemote extends Client {
}
